Both frontend and backend to CBT app.

frontend: ReactJS
backend: NodeJS, Express, MongoDB

to start, go into frontend/backend folders and run 'npm start'

frontend -> localhost:3000

backend -> localhost:5000
